class Env {
  static const String apiBaseUrl = 'http://172.20.1.172:8000/api/v1';
  static const bool isTestMode = false; // Set to false for production mode
  // Stripe configuration is now fetched dynamically from the backend
  // Add other environment variables here as needed
}
